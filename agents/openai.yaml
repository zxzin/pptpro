interface:
  display_name: "PPTPRO"
  short_description: "Create editable anti-AI decks with optional Zinx style"
  default_prompt: "Use $pptpro to create a polished editable deck, apply the anti-AI design gate, preserve any supplied template and source evidence, use Zinx presentation style only when requested, render previews, repair defects, and run the strict audit before handoff."
